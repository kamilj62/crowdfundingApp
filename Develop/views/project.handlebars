{{#each projects as |project| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{project.name}}</h2>
    <h5>{{project.description}}</h5>
    <h5>{{project.needed_funding}}</h5>
    <h5>{{project.date_created}}</h5>
  </div>

</div>
{{/each}}




